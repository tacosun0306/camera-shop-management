# 相機店管理系統 - 雲端部署指南

## 📋 部署方案

使用免費雲端服務部署系統：
- **後端（API）**: Render.com
- **前端（Web）**: Vercel.com
- **資料庫**: SQLite（檔案型，已包含）

---

## 🚀 快速部署步驟

### 第一步：準備 GitHub 倉庫

#### 1. 創建 GitHub 帳號（如果還沒有）
前往：https://github.com/signup

#### 2. 創建新倉庫
1. 登入 GitHub
2. 點擊右上角 `+` → `New repository`
3. 填寫資訊：
   - Repository name: `camera-shop-management`
   - Description: `相機店管理系統`
   - 選擇 `Public`（公開）
   - 不要勾選任何初始化選項
4. 點擊 `Create repository`

#### 3. 推送代碼到 GitHub

在專案目錄執行：

\`\`\`powershell
# 添加所有檔案
git add .

# 提交
git commit -m "Initial commit - 相機店管理系統"

# 連接到 GitHub（替換成你的倉庫網址）
git remote add origin https://github.com/你的用戶名/camera-shop-management.git

# 推送
git branch -M main
git push -u origin main
\`\`\`

---

### 第二步：部署後端到 Render

#### 1. 註冊 Render 帳號
前往：https://render.com/
- 使用 GitHub 帳號登入（推薦）

#### 2. 創建 Web Service

1. 點擊 `New +` → `Web Service`
2. 連接 GitHub 倉庫：
   - 點擊 `Connect` 找到 `camera-shop-management`
3. 填寫設定：
   - **Name**: `camera-shop-backend`
   - **Root Directory**: `backend`
   - **Runtime**: `Node`
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`
   - **Instance Type**: 選擇 `Free`

4. 環境變數（Environment）：
   點擊 `Advanced` → `Add Environment Variable`
   
   添加以下變數：
   \`\`\`
   NODE_ENV = production
   PORT = 3000
   JWT_SECRET = your-super-secret-jwt-key-change-this-in-production
   \`\`\`

5. 點擊 `Create Web Service`

6. 等待部署完成（約 3-5 分鐘）

7. **記下後端網址**，例如：
   \`\`\`
   https://camera-shop-backend.onrender.com
   \`\`\`

#### 3. 測試後端

在瀏覽器訪問：
\`\`\`
https://你的後端網址.onrender.com/health
\`\`\`

應該看到：
\`\`\`json
{"status":"ok","message":"相機店管理系統 API 運行中"}
\`\`\`

---

### 第三步：部署前端到 Vercel

#### 1. 註冊 Vercel 帳號
前往：https://vercel.com/signup
- 使用 GitHub 帳號登入（推薦）

#### 2. 匯入專案

1. 點擊 `Add New...` → `Project`
2. 找到並選擇 `camera-shop-management` 倉庫
3. 點擊 `Import`

#### 3. 配置專案

1. **Framework Preset**: 自動偵測為 `Vite`
2. **Root Directory**: 點擊 `Edit`，選擇 `frontend`
3. **Build Command**: `npm run build`（預設）
4. **Output Directory**: `dist`（預設）
5. **Install Command**: `npm install`（預設）

#### 4. 環境變數

點擊 `Environment Variables`，添加：

\`\`\`
VITE_API_URL = https://你的後端網址.onrender.com
\`\`\`

⚠️ **重要**：將 `你的後端網址` 替換成第二步記下的 Render 後端網址

#### 5. 修改前端 API 配置

**方法 1：使用環境變數（推薦）**

修改 \`frontend/src/api/axios.ts\`：

\`\`\`typescript
import axios from 'axios';

const api = axios.create({
  baseURL: import.meta.env.VITE_API_URL || '/api',
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json',
  },
});
\`\`\`

**方法 2：直接修改 Vite 配置**

修改 \`frontend/vite.config.ts\`：

\`\`\`typescript
export default defineConfig({
  plugins: [react()],
  server: {
    host: true,
    port: 5173,
    proxy: {
      '/api': {
        target: 'https://你的後端網址.onrender.com',  // 改成實際網址
        changeOrigin: true,
      },
    },
  },
});
\`\`\`

#### 6. 部署

1. 點擊 `Deploy`
2. 等待部署完成（約 2-3 分鐘）
3. **記下前端網址**，例如：
   \`\`\`
   https://camera-shop-management.vercel.app
   \`\`\`

---

## 🎉 完成！開始使用

### 訪問系統

**前端網址**：https://你的專案名稱.vercel.app

**登入資訊**：
- 帳號：`admin`
- 密碼：`admin123`

### 分享給測試人員

直接提供 Vercel 的前端網址即可，全世界任何地方都能訪問！

---

## ⚙️ 進階設定

### 自訂域名

#### Vercel（前端）
1. 到專案設定 → Domains
2. 添加自己的域名
3. 按照指示設定 DNS

#### Render（後端）
1. 到 Settings → Custom Domain
2. 添加域名並設定 DNS

### 資料庫持久化

⚠️ **重要**：Render 免費版每次重啟會清空檔案！

**解決方案：升級到付費版或使用雲端資料庫**

選項 1：使用 Render PostgreSQL（免費）
選項 2：使用 Supabase（免費）
選項 3：使用 PlanetScale（免費）

### 自動部署

已設定完成！每次推送代碼到 GitHub，系統會自動重新部署：

\`\`\`powershell
git add .
git commit -m "更新功能"
git push
\`\`\`

---

## 🔧 常見問題

### Q1: 後端部署失敗
**檢查項目**：
- Build Command 是否正確
- package.json 中是否有 build script
- 環境變數是否設定正確

### Q2: 前端無法連接後端
**解決方案**：
- 確認後端網址正確
- 檢查 CORS 設定
- 查看瀏覽器開發者工具的 Network 標籤

### Q3: 登入後資料消失
**原因**：Render 免費版重啟會清空資料庫
**解決**：使用付費版或雲端資料庫

### Q4: 部署後顯示 404
**前端**：檢查 vercel.json 的 rewrites 設定
**後端**：確認路由設定正確

### Q5: 環境變數不生效
**解決**：
- 重新部署專案
- 檢查變數名稱是否正確
- Vite 變數必須以 VITE_ 開頭

---

## 📊 成本估算

### 完全免費方案
- Render: 免費層（750 小時/月，單一專案足夠）
- Vercel: 免費層（個人用途，無限專案）
- 總成本：**$0/月**

⚠️ 限制：
- Render 閒置 15 分鐘後會休眠，首次訪問需約 30 秒啟動
- 每月 750 小時使用限制

### 付費方案（如需長期使用）
- Render: $7/月（無休眠）
- Vercel: 免費（足夠使用）
- 總成本：**$7/月**

---

## 🛠️ 備用方案

### 其他雲端平台

#### Railway.app
- 類似 Render
- $5/月起
- 設定更簡單

#### Fly.io
- 免費額度：3 個 VM
- 適合全球部署

#### Netlify
- 前端替代 Vercel
- 功能類似

---

## 📞 需要協助？

部署過程遇到問題？檢查：

1. **構建日誌**（Build Logs）- 查看錯誤訊息
2. **運行日誌**（Runtime Logs）- 查看執行錯誤
3. **環境變數** - 確認設定正確
4. **網路請求** - F12 開發者工具

---

**祝部署順利！** 🎊
